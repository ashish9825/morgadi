import 'package:flutter/material.dart';

class AnyCarItem {
  bool isSelected;
  final String buttonText;
  final Color color;
  final String assetImage;
  final Color assetColor;

  AnyCarItem(this.isSelected, this.buttonText, this.color, this.assetImage,
      this.assetColor);
}
